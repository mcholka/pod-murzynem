<%= simple_form_for(@porownaj) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :link %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
