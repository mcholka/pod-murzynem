<% title "Producenci" %>

<% @producents.each do |producent| %>
<article class="index">
  <div class="attribute">
  <% @id = producent.id %>
  <a href="<%=telefons_path(producent_id: @id.to_i) %>"><img src="<%= producent.link %>" height="375" width="500" /></a>
  <%= link_to '->', telefons_path(producent_id: @id.to_i) %>
  </div>
</article>
<% end %>
<%= paginate @producents %>

