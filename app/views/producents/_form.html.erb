<%= simple_form_for(@producent) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :nazwa %>
    <%= f.input :link %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
